import { Stack } from "@mui/material";

import CreateOrderForm from "./CreateOrderForm";

const CreateOrder = () => {
  return (
    <Stack gap={2}>
      <CreateOrderForm />
    </Stack>
  );
};

export default CreateOrder;
